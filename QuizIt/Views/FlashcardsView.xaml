<UserControl x:Class="QuizIt.Views.FlashcardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:QuizIt.Views">
    <StackPanel Margin="20">
        <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold"/>
        <TextBlock Text="Fiszki w tej talii:" Margin="0,10,0,5"/>

        <ListBox ItemsSource="{Binding Flashcards}" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Title}" FontSize="16" Width="200" VerticalAlignment="Center"/>
                        <Button Content="✏️ Edytuj pytania" Click="OpenFlashcard_Click" Margin="5,0"/>
                        <Button Content="✎ Tytuł" Click="EditFlashcard_Click" Margin="5,0"/>
                        <Button Content="🗑" Click="DeleteFlashcard_Click" Margin="5,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Text="Dodaj nową fiszkę:" Margin="10,20,0,5"/>
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="NewFlashcardTitleBox" Width="200" Margin="0,0,10,0"/>
            <Button Content="Dodaj" Click="AddFlashcard_Click"/>
        </StackPanel>
    </StackPanel>
</UserControl>
