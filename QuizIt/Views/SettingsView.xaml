<UserControl x:Class="QuizIt.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:QuizIt">
    <StackPanel Margin="20" VerticalAlignment="Top">
        <TextBlock Text="⚙️ Ustawienia" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Text="Imię użytkownika:"/>
        <TextBox x:Name="UsernameBox" Width="200" Margin="0,5,0,15"/>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <CheckBox x:Name="DarkModeCheckBox" Content="Włącz tryb ciemny" Checked="DarkModeChanged" Unchecked="DarkModeChanged"/>
        </StackPanel>

        <Button Content="Zapisz" Click="SaveSettings_Click" Width="100" Margin="0,20,0,0"/>
    </StackPanel>
</UserControl>
